name: Build Executables

on:
  push:
    branches:
      - develop
      - main
    tags:
      - 'v*'
  pull_request:
    branches:
      - main
      - develop
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build TypeScript
        run: npm run build

      - name: Create bin directory
        run: mkdir -p bin

      - name: Package for Windows
        run: npx pkg dist/index.js --targets node18-win-x64 --output bin/blip-list-generator-win.exe

      - name: Package for Linux
        run: npx pkg dist/index.js --targets node18-linux-x64 --output bin/blip-list-generator-linux

      - name: Package for macOS
        run: npx pkg dist/index.js --targets node18-macos-x64 --output bin/blip-list-generator-macos

      - name: List binaries
        run: ls -la bin/

      - name: Upload Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: blip-list-generator-windows
          path: bin/blip-list-generator-win.exe

      - name: Upload Linux artifact
        uses: actions/upload-artifact@v4
        with:
          name: blip-list-generator-linux
          path: bin/blip-list-generator-linux

      - name: Upload macOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: blip-list-generator-macos
          path: bin/blip-list-generator-macos

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4

      - name: List downloaded artifacts
        run: ls -la */

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            blip-list-generator-windows/blip-list-generator-win.exe
            blip-list-generator-linux/blip-list-generator-linux
            blip-list-generator-macos/blip-list-generator-macos
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
